---
import { resolveIconSvg } from '../lib/resolve-theme-icons';
import {
  accordionItemPropsSchema,
  accordionPropsSchema,
  backToTopPropsSchema,
  bannerPropsSchema,
  beforeAfterPropsSchema,
  breadcrumbPropsSchema,
  buttonPropsSchema,
  calloutPropsSchema,
  codePropsSchema,
  columnsPropsSchema,
  containerPropsSchema,
  countdownPropsSchema,
  embedHtmlPropsSchema,
  featureGridPropsSchema,
  featurePropsSchema,
  formBlockPropsSchema,
  galleryPropsSchema,
  hamburgerMenuPropsSchema,
  headingPropsSchema,
  heroPropsSchema,
  imagePropsSchema,
  imageSliderPropsSchema,
  languageSwitcherPropsSchema,
  linkPropsSchema,
  logoStripPropsSchema,
  mapEmbedPropsSchema,
  navDropdownPropsSchema,
  navLinkPropsSchema,
  navPropsSchema,
  newsletterSignupPropsSchema,
  pricingPlanPropsSchema,
  pricingTablePropsSchema,
  quotePropsSchema,
  ratingPropsSchema,
  searchBoxPropsSchema,
  statPropsSchema,
  statsCounterPropsSchema,
  tabPropsSchema,
  tabsPropsSchema,
  tablePropsSchema,
  teamMemberPropsSchema,
  teamPropsSchema,
  testimonialPropsSchema,
  testimonialsPropsSchema,
  timelinePropsSchema,
  timelineStepPropsSchema,
  promoBarPropsSchema,
  textPropsSchema,
  videoEmbedPropsSchema,
  whatsAppButtonPropsSchema,
  type Block,
  type PublishedPageAncestor,
  type PublishedPageTranslation,
  type PublishedSite,
} from '@brisk/shared-types';
import type { AstroComponentFactory } from 'astro/runtime/server/index.js';
import { resolveThemeBlockOverride } from '../lib/resolve-theme-block-override';
import { themeBlockRegistry } from '../lib/resolve-theme-page-blocks';

interface Props {
  block: Block;
  locale: string;
  translations: PublishedPageTranslation[];
  site: PublishedSite;
  ancestors: PublishedPageAncestor[];
  currentPageTitle: string;
  // Only the preview routes (apps/public-site/src/pages/preview/[pageId].astro)
  // pass true — the real published route never does, so `Wrapper` below
  // always stays `Fragment` and the published HTML is byte-identical to
  // before this prop existed (see the visual editor plan, Day 2).
  editable?: boolean;
}

const {
  block,
  locale,
  translations,
  site,
  ancestors,
  currentPageTitle,
  editable,
} = Astro.props;
// docs/adr/0042 — read here, before any of the resolveThemeBlockOverride
// calls below, since every one of them needs it. Derived from `site`
// (already threaded through every recursive <Astro.self> call further
// down) rather than a second, parallel prop — a block never needs a
// theme different from the site it's rendering for.
const themeName = site.themeName;

import AccordionCore from './blocks/Accordion.astro';
const Accordion = resolveThemeBlockOverride('Accordion', AccordionCore, themeName);
import AccordionItemCore from './blocks/AccordionItem.astro';
const AccordionItem = resolveThemeBlockOverride('AccordionItem', AccordionItemCore, themeName);
import BackToTopCore from './blocks/BackToTop.astro';
const BackToTop = resolveThemeBlockOverride('BackToTop', BackToTopCore, themeName);
import BannerCore from './blocks/Banner.astro';
const Banner = resolveThemeBlockOverride('Banner', BannerCore, themeName);
import BeforeAfterCore from './blocks/BeforeAfter.astro';
const BeforeAfter = resolveThemeBlockOverride('BeforeAfter', BeforeAfterCore, themeName);
import BreadcrumbCore from './blocks/Breadcrumb.astro';
const Breadcrumb = resolveThemeBlockOverride('Breadcrumb', BreadcrumbCore, themeName);
import ButtonCore from './blocks/Button.astro';
const Button = resolveThemeBlockOverride('Button', ButtonCore, themeName);
import CalloutCore from './blocks/Callout.astro';
const Callout = resolveThemeBlockOverride('Callout', CalloutCore, themeName);
import CodeCore from './blocks/Code.astro';
const Code = resolveThemeBlockOverride('Code', CodeCore, themeName);
import ColumnCore from './blocks/Column.astro';
const Column = resolveThemeBlockOverride('Column', ColumnCore, themeName);
import ColumnsCore from './blocks/Columns.astro';
const Columns = resolveThemeBlockOverride('Columns', ColumnsCore, themeName);
import ContainerCore from './blocks/Container.astro';
const Container = resolveThemeBlockOverride('Container', ContainerCore, themeName);
import CountdownCore from './blocks/Countdown.astro';
const Countdown = resolveThemeBlockOverride('Countdown', CountdownCore, themeName);
import EmbedHtmlCore from './blocks/EmbedHtml.astro';
const EmbedHtml = resolveThemeBlockOverride('EmbedHtml', EmbedHtmlCore, themeName);
import FeatureCore from './blocks/Feature.astro';
const Feature = resolveThemeBlockOverride('Feature', FeatureCore, themeName);
import FeatureGridCore from './blocks/FeatureGrid.astro';
const FeatureGrid = resolveThemeBlockOverride('FeatureGrid', FeatureGridCore, themeName);
import FormCore from './blocks/Form.astro';
const Form = resolveThemeBlockOverride('Form', FormCore, themeName);
import GalleryCore from './blocks/Gallery.astro';
const Gallery = resolveThemeBlockOverride('Gallery', GalleryCore, themeName);
import HamburgerMenuCore from './blocks/HamburgerMenu.astro';
const HamburgerMenu = resolveThemeBlockOverride('HamburgerMenu', HamburgerMenuCore, themeName);
import HeroCore from './blocks/Hero.astro';
const Hero = resolveThemeBlockOverride('Hero', HeroCore, themeName);
import HeadingCore from './blocks/Heading.astro';
const Heading = resolveThemeBlockOverride('Heading', HeadingCore, themeName);
import ImageCore from './blocks/Image.astro';
const Image = resolveThemeBlockOverride('Image', ImageCore, themeName);
import ImageSliderCore from './blocks/ImageSlider.astro';
const ImageSlider = resolveThemeBlockOverride('ImageSlider', ImageSliderCore, themeName);
import LanguageSwitcherCore from './blocks/LanguageSwitcher.astro';
const LanguageSwitcher = resolveThemeBlockOverride('LanguageSwitcher', LanguageSwitcherCore, themeName);
import LinkCore from './blocks/Link.astro';
const Link = resolveThemeBlockOverride('Link', LinkCore, themeName);
import LogoStripCore from './blocks/LogoStrip.astro';
const LogoStrip = resolveThemeBlockOverride('LogoStrip', LogoStripCore, themeName);
import MapEmbedCore from './blocks/MapEmbed.astro';
const MapEmbed = resolveThemeBlockOverride('MapEmbed', MapEmbedCore, themeName);
import NavCore from './blocks/Nav.astro';
const Nav = resolveThemeBlockOverride('Nav', NavCore, themeName);
import NavDropdownCore from './blocks/NavDropdown.astro';
const NavDropdown = resolveThemeBlockOverride('NavDropdown', NavDropdownCore, themeName);
import NavLinkCore from './blocks/NavLink.astro';
const NavLink = resolveThemeBlockOverride('NavLink', NavLinkCore, themeName);
import NewsletterSignupCore from './blocks/NewsletterSignup.astro';
const NewsletterSignup = resolveThemeBlockOverride('NewsletterSignup', NewsletterSignupCore, themeName);
import PricingPlanCore from './blocks/PricingPlan.astro';
const PricingPlan = resolveThemeBlockOverride('PricingPlan', PricingPlanCore, themeName);
import PricingTableCore from './blocks/PricingTable.astro';
const PricingTable = resolveThemeBlockOverride('PricingTable', PricingTableCore, themeName);
import QuoteCore from './blocks/Quote.astro';
const Quote = resolveThemeBlockOverride('Quote', QuoteCore, themeName);
import RatingCore from './blocks/Rating.astro';
const Rating = resolveThemeBlockOverride('Rating', RatingCore, themeName);
import StatCore from './blocks/Stat.astro';
const Stat = resolveThemeBlockOverride('Stat', StatCore, themeName);
import StatsCounterCore from './blocks/StatsCounter.astro';
const StatsCounter = resolveThemeBlockOverride('StatsCounter', StatsCounterCore, themeName);
import TabCore from './blocks/Tab.astro';
const Tab = resolveThemeBlockOverride('Tab', TabCore, themeName);
import TabsCore from './blocks/Tabs.astro';
const Tabs = resolveThemeBlockOverride('Tabs', TabsCore, themeName);
import TableCore from './blocks/Table.astro';
const Table = resolveThemeBlockOverride('Table', TableCore, themeName);
import TeamCore from './blocks/Team.astro';
const Team = resolveThemeBlockOverride('Team', TeamCore, themeName);
import TeamMemberCore from './blocks/TeamMember.astro';
const TeamMember = resolveThemeBlockOverride('TeamMember', TeamMemberCore, themeName);
import TestimonialCore from './blocks/Testimonial.astro';
const Testimonial = resolveThemeBlockOverride('Testimonial', TestimonialCore, themeName);
import TestimonialsCore from './blocks/Testimonials.astro';
const Testimonials = resolveThemeBlockOverride('Testimonials', TestimonialsCore, themeName);
import TimelineCore from './blocks/Timeline.astro';
const Timeline = resolveThemeBlockOverride('Timeline', TimelineCore, themeName);
import TimelineStepCore from './blocks/TimelineStep.astro';
const TimelineStep = resolveThemeBlockOverride('TimelineStep', TimelineStepCore, themeName);
import PromoBarCore from './blocks/PromoBar.astro';
const PromoBar = resolveThemeBlockOverride('PromoBar', PromoBarCore, themeName);
import SearchBoxCore from './blocks/SearchBox.astro';
const SearchBox = resolveThemeBlockOverride('SearchBox', SearchBoxCore, themeName);
import TextCore from './blocks/Text.astro';
const Text = resolveThemeBlockOverride('Text', TextCore, themeName);
import VideoEmbedCore from './blocks/VideoEmbed.astro';
const VideoEmbed = resolveThemeBlockOverride('VideoEmbed', VideoEmbedCore, themeName);
import WhatsAppButtonCore from './blocks/WhatsAppButton.astro';
const WhatsAppButton = resolveThemeBlockOverride('WhatsAppButton', WhatsAppButtonCore, themeName);

// Walks `Block[]`/`children` directly — no dependency on Puck's own data
// format here, by design (see docs/adr/0007: "any consumer of page content
// that isn't the editor... can walk children directly without knowing
// anything about Puck"). Each block's own Zod schema (shared with
// @brisk/puck-config, same source of truth) validates `props` at this
// boundary instead of an `as` cast — content comes from the API as
// unvalidated JSON as far as this component is concerned.
//
// `locale`/`translations`/`site` are only used by LanguageSwitcher, which
// can be nested inside a Nav (docs/adr/0018) — threaded through every
// recursive call rather than looked up again per block, since a leaf has
// no way to reach back up to the page-level values otherwise.
// `ancestors`/`currentPageTitle` follow the same reasoning for Breadcrumb.

interface BlockRenderContext {
  locale: string;
  translations: PublishedPageTranslation[];
  site: PublishedSite;
  ancestors: PublishedPageAncestor[];
  currentPageTitle: string;
  editable?: boolean;
}

/**
 * One entry for each of the 48 block types — replacing the old
 * `{block.type === 'X' && (<X .../>)}` dispatch repeated 48 times (see this
 * file's git history). The variation between types is entirely STATIC
 * metadata known when the file is written (5 independent facts: does it
 * have styleOverride? a locale? container children? ancestors/
 * currentPageTitle? translations/site?), not logic computed at runtime —
 * hence a data table rather than preserving the conditional. `extra`
 * remains the one escape hatch, used only by the 2 types (Breadcrumb,
 * LanguageSwitcher) with props genuinely unique to them.
 *
 * `<Component />` with `Component` a variable resolved at runtime from this
 * table is the same pattern `Wrapper` just below already uses
 * (`editable ? 'div' : Fragment`) — not something new for Astro in this
 * project, just applied to a set of 48 rather than of 2.
 */
interface BlockDispatchEntry {
  component: AstroComponentFactory;
  schema?: { parse: (props: unknown) => Record<string, unknown> };
  styleOverride?: boolean;
  locale?: boolean;
  /** Recurses into `block.children` — true for Nav/NavDropdown/HamburgerMenu/Tab too, which however do NOT receive `editable`/`hasChildren` (they are not "slot" containers in the same sense). */
  recurseChildren?: boolean;
  /** Real containers only (Columns/Container/Accordion/etc.) — they also receive `editable`+`hasChildren` on top of recursing into their children. */
  containerProps?: boolean;
  extra?: (context: BlockRenderContext) => Record<string, unknown>;
}

const BLOCK_REGISTRY: Record<string, BlockDispatchEntry> = {
  Hero: { component: Hero, schema: heroPropsSchema, styleOverride: true },
  Heading: {
    component: Heading,
    schema: headingPropsSchema,
    styleOverride: true,
  },
  Text: { component: Text, schema: textPropsSchema, styleOverride: true },
  Image: { component: Image, schema: imagePropsSchema, locale: true },
  Gallery: { component: Gallery, schema: galleryPropsSchema, locale: true },
  Form: {
    component: Form,
    schema: formBlockPropsSchema,
    styleOverride: true,
    locale: true,
  },
  Quote: { component: Quote, schema: quotePropsSchema, styleOverride: true },
  Callout: { component: Callout, schema: calloutPropsSchema },
  Rating: { component: Rating, schema: ratingPropsSchema, styleOverride: true },
  Countdown: {
    component: Countdown,
    schema: countdownPropsSchema,
    styleOverride: true,
    locale: true,
  },
  EmbedHtml: {
    component: EmbedHtml,
    schema: embedHtmlPropsSchema,
    styleOverride: true,
  },
  Code: {
    component: Code,
    schema: codePropsSchema,
    styleOverride: true,
  },
  Table: { component: Table, schema: tablePropsSchema },
  NavLink: { component: NavLink, schema: navLinkPropsSchema },
  LanguageSwitcher: {
    component: LanguageSwitcher,
    schema: languageSwitcherPropsSchema,
    locale: true,
    extra: (ctx) => ({ translations: ctx.translations, site: ctx.site }),
  },
  Breadcrumb: {
    component: Breadcrumb,
    schema: breadcrumbPropsSchema,
    locale: true,
    extra: (ctx) => ({
      ancestors: ctx.ancestors,
      currentPageTitle: ctx.currentPageTitle,
    }),
  },
  SearchBox: { component: SearchBox, schema: searchBoxPropsSchema, locale: true },
  Nav: { component: Nav, schema: navPropsSchema, recurseChildren: true },
  HamburgerMenu: {
    component: HamburgerMenu,
    schema: hamburgerMenuPropsSchema,
    locale: true,
    recurseChildren: true,
  },
  NavDropdown: {
    component: NavDropdown,
    schema: navDropdownPropsSchema,
    recurseChildren: true,
  },
  Columns: {
    component: Columns,
    schema: columnsPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  Column: {
    component: Column,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  Container: {
    component: Container,
    schema: containerPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  PromoBar: {
    component: PromoBar,
    schema: promoBarPropsSchema,
    styleOverride: true,
    locale: true,
  },
  BackToTop: { component: BackToTop, schema: backToTopPropsSchema, locale: true },
  WhatsAppButton: {
    component: WhatsAppButton,
    schema: whatsAppButtonPropsSchema,
    locale: true,
  },
  Accordion: {
    component: Accordion,
    schema: accordionPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  AccordionItem: {
    component: AccordionItem,
    schema: accordionItemPropsSchema,
    styleOverride: true,
  },
  Tabs: {
    component: Tabs,
    schema: tabsPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  Tab: {
    component: Tab,
    schema: tabPropsSchema,
    styleOverride: true,
    recurseChildren: true,
  },
  Banner: { component: Banner, schema: bannerPropsSchema, styleOverride: true },
  Button: { component: Button, schema: buttonPropsSchema, styleOverride: true },
  Link: { component: Link, schema: linkPropsSchema },
  FeatureGrid: {
    component: FeatureGrid,
    schema: featureGridPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  Feature: { component: Feature, schema: featurePropsSchema, styleOverride: true },
  VideoEmbed: { component: VideoEmbed, schema: videoEmbedPropsSchema, locale: true },
  MapEmbed: { component: MapEmbed, schema: mapEmbedPropsSchema, locale: true },
  ImageSlider: {
    component: ImageSlider,
    schema: imageSliderPropsSchema,
    locale: true,
  },
  BeforeAfter: {
    component: BeforeAfter,
    schema: beforeAfterPropsSchema,
    locale: true,
  },
  LogoStrip: {
    component: LogoStrip,
    schema: logoStripPropsSchema,
    styleOverride: true,
    locale: true,
  },
  Testimonials: {
    component: Testimonials,
    schema: testimonialsPropsSchema,
    styleOverride: true,
    locale: true,
    recurseChildren: true,
    containerProps: true,
  },
  Testimonial: {
    component: Testimonial,
    schema: testimonialPropsSchema,
    styleOverride: true,
  },
  Team: {
    component: Team,
    schema: teamPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  TeamMember: {
    component: TeamMember,
    schema: teamMemberPropsSchema,
    styleOverride: true,
  },
  PricingTable: {
    component: PricingTable,
    schema: pricingTablePropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  PricingPlan: {
    component: PricingPlan,
    schema: pricingPlanPropsSchema,
    styleOverride: true,
  },
  StatsCounter: {
    component: StatsCounter,
    schema: statsCounterPropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  Stat: { component: Stat, schema: statPropsSchema, styleOverride: true },
  Timeline: {
    component: Timeline,
    schema: timelinePropsSchema,
    styleOverride: true,
    recurseChildren: true,
    containerProps: true,
  },
  TimelineStep: { component: TimelineStep, schema: timelineStepPropsSchema },
  NewsletterSignup: {
    component: NewsletterSignup,
    schema: newsletterSignupPropsSchema,
    locale: true,
  },
};
// Docs/adr/0041 — the one, one-time edit this file needs for a theme to
// add its own genuinely new block types (`themes/<name>/blocks/*.block.ts`)
// with zero further changes here going forward: every other line above is
// untouched by this. `Object.assign` (not a `const` reassignment) since
// `BLOCK_REGISTRY` stays a single object identity referenced by the
// dispatch code below.
Object.assign(
  BLOCK_REGISTRY,
  themeBlockRegistry(Object.keys(BLOCK_REGISTRY), themeName),
);

// `Astro.slots.has('default')` is not enough for a freshly created empty
// container: canvas-editor-shell.tsx gives it `children: []` (an empty
// array, not `undefined`) — passing that through as children (even though
// `.map` produces an empty array) makes the slot count as "filled" for
// Astro, even when it renders nothing visible. The real count has to come
// from the `Block` itself, not from the slot.
const hasChildren = Boolean(block.children?.length);

// A single wrapping point for the whole component (not one scattered
// through the ~50 arms below) — a block is always exactly one of those
// arms, never more than one, so wrapping the entire output is equivalent to
// wrapping "the" block. `display:contents` avoids by construction the "a
// real wrapper breaks the CSS grid" bug this codebase already hit once
// (Column.astro, min-width:auto) — a real wrapper here (a <div> without
// display:contents, say) would break the layout of Columns/FeatureGrid/etc.
// A known consequence: getBoundingClientRect() on a display:contents node
// is empty — preview-bridge-client.ts (read by init-preview-bridge.ts) uses Range.selectNodeContents instead.
const Wrapper = editable ? 'div' : Fragment;
const wrapperProps = editable
  ? {
      'data-brisk-block-id': block.id,
      'data-brisk-block-type': block.type,
      style: 'display:contents',
    }
  : {};

const entry = BLOCK_REGISTRY[block.type];
const Component = entry?.component;
const context: BlockRenderContext = {
  locale,
  translations,
  site,
  ancestors,
  currentPageTitle,
  editable,
};
const parsedProps: Record<string, unknown> = entry?.schema
  ? entry.schema.parse(block.props)
  : {};
// Resolved here, once, and handed down as `iconSvg` — a block (core's or a
// theme's override of it) never reaches into the icon registry itself.
// That registry is apps-only (eager glob + lucide-static via
// import.meta.resolve), so a theme override that had to call it could not
// live outside this repo. Only blocks whose schema actually has an `icon`
// field get the prop; for everything else this is a `typeof` check and
// nothing more.
const iconName =
  typeof parsedProps['icon'] === 'string' ? parsedProps['icon'] : null;
const dispatchProps = entry
  ? {
      ...parsedProps,
      ...(entry.styleOverride ? { styleOverride: block.styleOverride } : {}),
      ...(entry.locale ? { locale } : {}),
      ...(entry.containerProps ? { editable, hasChildren } : {}),
      // docs/adr/0042 — always included, not gated by a dispatch-entry
      // flag like styleOverride/locale above: cheap to pass to every
      // block (a plain string already resolved once per render), and
      // more than one block type may want it over time (NavLink/Feature
      // do today, for icon resolution) without this table needing a new
      // capability flag added per consumer.
      themeName,
      ...(iconName ? { iconSvg: resolveIconSvg(iconName, themeName) } : {}),
      ...(entry.extra ? entry.extra(context) : {}),
    }
  : {};
---

<Wrapper {...wrapperProps}>
{
  Component && entry && (
    <Component {...dispatchProps}>
      {entry.recurseChildren &&
        block.children?.map((child) => (
          <Astro.self
            block={child}
            locale={locale}
            translations={translations}
            site={site}
            ancestors={ancestors}
            currentPageTitle={currentPageTitle}
            editable={editable}
          />
        ))}
    </Component>
  )
}
</Wrapper>
