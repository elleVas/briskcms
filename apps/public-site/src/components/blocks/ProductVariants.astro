---
import type { ProductVariantsProps } from '@brisk/shared-types';
import { Translator } from '@brisk/theme-runtime';

type Props = ProductVariantsProps & {
  locale: string;
  editable?: boolean;
  instanceClass?: string | null;
};

const { label, options, locale, editable, instanceClass } = Astro.props;
const i18n = new Translator(locale);
const values = options
  .split('\n')
  .map((option) => option.trim())
  .filter(Boolean);
---

{
  values.length > 0 ? (
    <div class:list={['brisk-product-variants', instanceClass]}>
      {label && <p class="brisk-product-variants__label">{label}</p>}
      {/* A list, not buttons: nothing happens when one is chosen, so
          nothing here offers to be chosen. */}
      <ul class="brisk-product-variants__options" aria-label={label || undefined}>
        {values.map((value) => (
          <li class="brisk-product-variants__option">{value}</li>
        ))}
      </ul>
    </div>
  ) : (
    editable && (
      <p class="brisk-product-variants brisk-product-variants--empty">
        {i18n.t('productVariants.empty')}
      </p>
    )
  )
}

<style>
  .brisk-product-variants {
    display: grid;
    gap: 0.5rem;
    background: var(--brisk-override-bg, transparent);
    color: var(--brisk-override-text, inherit);
    border-radius: var(--brisk-override-radius, 0);
    padding: var(--brisk-override-padding-y, 0)
      var(--brisk-override-padding-x, 0);
  }

  .brisk-product-variants__label {
    margin: 0;
    font-size: 0.875rem;
    font-weight: 600;
  }

  .brisk-product-variants__options {
    display: flex;
    flex-wrap: wrap;
    gap: var(--brisk-override-gap, 0.5rem);
    margin: 0;
    padding: 0;
    list-style: none;
  }

  .brisk-product-variants__option {
    padding: 0.25rem 0.75rem;
    border: 1px solid var(--border);
    border-radius: 999px;
    font-size: 0.875rem;
  }

  .brisk-product-variants--empty {
    font-size: 0.875rem;
    color: var(--muted-foreground);
  }
</style>
