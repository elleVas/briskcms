---
import {
  STAR_ICON_PATH,
  type RatingProps,
} from '@brisk/shared-types';
import { Translator } from '@brisk/theme-runtime';

type Props = RatingProps & { locale?: string; instanceClass?: string | null };

const { rating, label, locale, instanceClass } = Astro.props;
// Said in the page's language: this was Italian on every site, so an
// English page's stars were announced as "5 su 5 stelle".
const i18n = new Translator(locale ?? '');

const STAR_POSITIONS = [1, 2, 3, 4, 5];
---

<div class:list={['brisk-rating', instanceClass]}>
  {label && <p class="brisk-rating__label">{label}</p>}
  <div
    class="brisk-rating__stars"
    role="img"
    aria-label={i18n.t('rating.label', { value: rating })}
  >
    {
      STAR_POSITIONS.map((position) => (
        // Filled stars stay literal amber regardless of theme — a rating
        // color, not a brand one, same reasoning as WhatsAppButton's green.
        <svg viewBox="0 0 20 20" width="24" height="24" aria-hidden="true">
          <path
            d={STAR_ICON_PATH}
            fill={position <= rating ? '#f59e0b' : 'var(--border)'}
          />
        </svg>
      ))
    }
  </div>
</div>

<style>
  .brisk-rating {
    background: var(--brisk-override-bg, transparent);
    color: var(--brisk-override-text, inherit);
    border-radius: var(--brisk-override-radius, 0);
    padding: var(--brisk-override-padding-y, 0) var(--brisk-override-padding-x, 0);
  }
  .brisk-rating__stars {
    display: flex;
    gap: 4px;
  }
</style>
