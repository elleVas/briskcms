---
import type { QuoteProps } from '@brisk/shared-types';

type Props = QuoteProps & { instanceClass?: string | null };

const { quote, author, role, instanceClass } = Astro.props;
const hasAuthor = Boolean(author);
const hasRole = Boolean(role);
---

<div class:list={['brisk-quote', instanceClass]}>
  <blockquote class="brisk-rich-text" data-brisk-field="quote" set:html={quote} />
  {
    (hasAuthor || hasRole) && (
      <cite>
        {hasAuthor && <span data-brisk-field="author">{author}</span>}
        {hasAuthor && hasRole && ' — '}
        {hasRole && <span data-brisk-field="role">{role}</span>}
      </cite>
    )
  }
</div>

<style>
  .brisk-quote {
    background: var(--brisk-override-bg, transparent);
    border-radius: var(--brisk-override-radius, 0);
    padding: var(--brisk-override-padding-y, 0) var(--brisk-override-padding-x, 0);
    color: var(--brisk-override-text, inherit);
  }
</style>
